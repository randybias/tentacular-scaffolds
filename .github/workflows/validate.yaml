name: Validate Scaffolds

on:
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install yq
        run: |
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq

      - name: Validate scaffold.yaml files exist and are valid YAML
        run: |
          set -euo pipefail
          errors=0
          for dir in quickstarts/*/; do
            name="$(basename "$dir")"
            scaffold="$dir/scaffold.yaml"
            wf="$dir/workflow.yaml"

            if [ ! -f "$scaffold" ]; then
              echo "ERROR: $scaffold is missing"
              errors=$((errors + 1))
              continue
            fi

            if ! yq eval '.' "$scaffold" > /dev/null 2>&1; then
              echo "ERROR: $scaffold is not valid YAML"
              errors=$((errors + 1))
            fi

            if [ ! -f "$wf" ]; then
              echo "ERROR: $wf is missing"
              errors=$((errors + 1))
            fi
          done

          if [ "$errors" -gt 0 ]; then
            echo "Validation failed with $errors error(s)"
            exit 1
          fi

          echo "All scaffolds validated successfully"

      - name: Verify build-index.sh runs without error
        run: |
          chmod +x ./scripts/build-index.sh
          ./scripts/build-index.sh
